name: Deploy
on:
  push:
    branches:
      - s3
jobs:
  deploykeun:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2
      - name: Publish
        uses: cloudflare/wrangler-action@1.21.0
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          command: publish 